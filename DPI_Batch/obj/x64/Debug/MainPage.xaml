<Page
    x:Class="DPI_Batch.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DPI_Batch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MyToolkit.Controls"
    xmlns:models="using:DPI_Batch.Models"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:ConnectionId='1' x:Name="BtnRawdata" Content="Choose rawdata" HorizontalAlignment="Left" VerticalAlignment="Top"                              />
        <Grid Grid.Row="1">
            
            <controls:DataGrid x:ConnectionId='2' ItemsSource="{Binding People}" x:Name="DataGrid" SelectionMode="Multiple" 
                               SelectedItem="{Binding SelectedPerson, Mode=TwoWay}" 
                               DefaultOrderIndex="1" Margin="0,51,0,-51">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Width="200" Header="CustomerId" Binding="{Binding CustomerId}"
                                                 Style="{StaticResource BaseTextBlockStyle}"  IsAscendingDefault="true"
                                                 d:DataContext="{d:DesignInstance Type=models:Person}" />
                    <controls:DataGridTextColumn Width="200" Header="Full name" Binding="{Binding FullName}"
                                                 Style="{StaticResource BaseTextBlockStyle}" 
                                                 d:DataContext="{d:DesignInstance Type=models:Person}" />
                    <controls:DataGridTextColumn Width="200"  Binding="{Binding SocialSecurityNumber}" Header="SocialSecurityNumber" 
                                                 Style="{StaticResource BaseTextBlockStyle}" 
                                                 d:DataContext="{d:DesignInstance Type=models:Person}">
                    </controls:DataGridTextColumn>
                    <controls:DataGridTextColumn Width="200" Header="ReservedForDigitallyDelivery" Binding="{Binding ReservedForDigitallyDelivery}" 
                                                 Style="{StaticResource BaseTextBlockStyle}" 
                                                 d:DataContext="{d:DesignInstance Type=models:Person}" />
                    <controls:DataGridTextColumn Width="200" Header="Status" Binding="{Binding Status}" 
                                                 Style="{StaticResource BaseTextBlockStyle}" 
                                                 d:DataContext="{d:DesignInstance Type=models:Person}" />
                </controls:DataGrid.Columns>
                <controls:DataGrid.ItemDetailsTemplate>
                    <DataTemplate>

                        <StackPanel Margin="10,10,10,5">
                            <TextBlock Text="Details: " Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" />
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="CustomerID : "></TextBlock>
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" Text="{Binding CustomerId}"/>
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="Subject : "></TextBlock>
                            <TextBlock Text="{Binding Subject}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="AddressLine1 : "></TextBlock>
                                <TextBlock Text="{Binding AddressLine1}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="AddressLine2 : "></TextBlock>
                                <TextBlock Text="{Binding AddressLine2}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="PostalNumber : "></TextBlock>
                                <TextBlock Text="{Binding PostalNumber}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="PostalPlace : "></TextBlock>
                                <TextBlock Text="{Binding PostalPlace}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="MobileNumber : "></TextBlock>
                                <TextBlock Text="{Binding MobileNumber}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="Email : "></TextBlock>
                                <TextBlock Text="{Binding Email}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="FileName : "></TextBlock>
                                <TextBlock Text="{Binding FileName}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="Attachment1 : "></TextBlock>
                                <TextBlock Text="{Binding Attachment1}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="Attachment2 : "></TextBlock>
                                <TextBlock Text="{Binding Attachment2}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="OrganizationNumber : "></TextBlock>
                                <TextBlock Text="{Binding OrganizationNumber}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="Country : "></TextBlock>
                                <TextBlock Text="{Binding Country}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="Kid : "></TextBlock>
                                <TextBlock Text="{Binding Kid}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="AccountNumber : "></TextBlock>
                                <TextBlock Text="{Binding AccountNumber}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="AmountDecimal : "></TextBlock>
                                <TextBlock Text="{Binding AmountDecimal}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel  Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Text="DueDate : "></TextBlock>
                                <TextBlock Text="{Binding DueDate}" Style="{StaticResource BaseTextBlockStyle}" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </controls:DataGrid.ItemDetailsTemplate>
            </controls:DataGrid>
            <Button x:ConnectionId='3' x:Name="BtnOppslagstjeneste" Content="Wash list" HorizontalAlignment="Left" Margin="156,0,0,0" VerticalAlignment="Top"                                  />
            <Button x:ConnectionId='4' x:Name="BtnSend" Content="Send" HorizontalAlignment="Left" Margin="265,0,0,0" VerticalAlignment="Top" IsEnabled="False"/>
        </Grid>

    </Grid>
</Page>

